@model List<Category>

@{
    ViewBag.Title = "Categories";
}

<div class="container">
    <div class="row">
        <h2>@ViewBag.Title</h2>

        @*<div class="col-md-10">
            <table class="table table-striped table-bordered table-condensed">
                <tr>
                    <th>Category Id</th>
                    <th>Category Name</th>
                    <th></th>
                    <th></th>
                </tr>
                @using (Html.BeginForm("GetCategories", "Polls", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
                {
                    foreach (Category cat in Model)
                    {
                        <tr>
                            <td>@cat.Id</td>
                            <td>@cat.Name</td>
                            <td>@Html.ActionLink("Edit", "EditCategory", "Polls", new { @class = "btn btn-primary btn-large" })</td>
                            <td>@Html.ActionLink("Delete", "DeleteCategory", "Polls", new { @class = "btn btn-primary btn-large" })</td>
                        </tr>
                    }
                }
            </table>
        </div>*@
    </div>
</div>

